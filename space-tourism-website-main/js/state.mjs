import { DATA } from './data.mjs';

export const STATE = {
	currentPlanetIndex: 0,
	currentMemberIndex: 0,
	currentTechIndex: 0,
	sections: [],

	// currentPlanet: DATA.destinations.at(currentPlanetIndex),
	// currentMember: DATA.crew.at(currentMemberIndex),
	// currentTech: DATA.technology.at(currentTechIndex),
};
